import React from 'react';

import './activity.css';

/* eslint-disable-next-line */
export interface ActivityProps {}

export const Activity = (props: ActivityProps) => {
  return (
    <div>
      <h1>Welcome to activity component!</h1>
    </div>
  );
};

export default Activity;
